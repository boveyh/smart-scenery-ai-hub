[project]
name = "smart-scenery-digital-human"
version = "3.0.0"
description = "Live2D 数字人配置模块 — 多租户人物设定 + 对接指南"
requires-python = ">=3.10"
dependencies = [
    # ─── 轻量级：仅配置加载，无需 GPU ───
    "pyyaml>=6.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]